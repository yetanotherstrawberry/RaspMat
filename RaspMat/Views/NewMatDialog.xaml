<local:WindowBase x:Class="RaspMat.Views.NewMatDialog"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:RaspMat.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prop="clr-namespace:RaspMat.Properties"
             xmlns:local="clr-namespace:RaspMat.Views"
             mc:Ignorable="d"
             Width="250"
             MinWidth="180"
             Height="150"
             MinHeight="140"
             d:DataContext="{d:DesignInstance vm:NewMatDialogViewModel, IsDesignTimeCreatable=False}"
             d:Background="White"
             Title="{x:Static prop:Resources.ENTER_MATRIX}">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Label Content="{x:Static prop:Resources.IN_ROWS}" />
                <TextBox Height="25" Text="{Binding Rows}" Margin="1" PreviewTextInput="TextBox_PreviewTextInput" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <Label Content="{x:Static prop:Resources.IN_COLUMNS}" />
                <TextBox Height="25" Text="{Binding Columns}" Margin="1" PreviewTextInput="TextBox_PreviewTextInput" />
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <Label Content="{x:Static prop:Resources.FILL}" />
                <TextBox Height="25" Text="{Binding Fill}" Margin="1" PreviewTextInput="TextBox_PreviewTextInput" Width="40" />
            </StackPanel>
            <Button Command="{Binding CloseDialogCommand}" Content="{x:Static prop:Resources.OK}" Margin="5" />
        </StackPanel>
    </Grid>
</local:WindowBase>
