<local:WindowBase
        x:Class="RaspMat.Views.BasisChangeWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RaspMat.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:prop="clr-namespace:RaspMat.Properties"
        xmlns:local="clr-namespace:RaspMat.Views"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance {x:Type vm:BasisChangeWindowsViewModel}, IsDesignTimeCreatable=False}" 
        Title="{x:Static prop:Resources.BASIS_CHANGE}"
        Height="450"
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="pack://application:,,,/RaspMat;component/Resources/Styles.xaml" />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>
        <ProgressBar
            Margin="3"
            Grid.Row="0"
            Height="20"
            Minimum="0"
            Maximum="3"
            Value="0"
            d:Value="1" />
        <StackPanel
            Grid.Row="1"
            Margin="3">
        </StackPanel>
        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="3">
            <Button
                Content="Back" />
            <Button
                Content="Next" />
        </StackPanel>
    </Grid>
</local:WindowBase>
