<local:DialogWindowBase x:Class="RaspMat.Views.NewVectorsDialog"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:RaspMat.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prop="clr-namespace:RaspMat.Properties"
             xmlns:local="clr-namespace:RaspMat.Views"
             mc:Ignorable="d"
             Width="400"
             Height="170"
             ResizeMode="NoResize"
             d:DataContext="{d:DesignInstance vm:NewMatrixDialogViewModel, IsDesignTimeCreatable=False}"
             d:Background="White"
             Title="{x:Static prop:Resources.NEW_VECTORS_TITLE}"
             IsEnabled="{Binding IsFree}">
    <Grid Margin="5">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="pack://application:,,,/RaspMat;component/Resources/Styles.xaml" />
                </ResourceDictionary.MergedDictionaries>

                <Style TargetType="TextBlock" /><!-- Override style. -->
            </ResourceDictionary>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid
            HeadersVisibility="None"
            CanUserAddRows="True"
            AutoGenerateColumns="False"
            d:ItemsSource="{d:SampleData ItemCount=5}">
            <DataGrid.Columns>
                <DataGridTextColumn />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <Button Command="{Binding CloseDialogCommand}" Content="{x:Static prop:Resources.OK}" Margin="5" />
        </StackPanel>
    </Grid>
</local:DialogWindowBase>
